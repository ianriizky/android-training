<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.co.terraresta.androidlesson">

    <!-- internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera and storage permission -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

   <uses-permission android:name="android.permission.CAMERA" />
    <!--<uses-feature android:name="android.hardware.camera" android:required="true"/>-->
    <!--<uses-feature android:name="android.hardware.camera.autofocus" />-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="jp.co.terraresta.androidlesson.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemeNoAction">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.common.MainTabActivity" />
        <activity
            android:name=".view.activity.sign_up.SignUpActivity"
            android:label="@string/sign_up_title"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".view.activity.login.LoginActivity"
            android:label="@string/login_title"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".WebViewActivity">
        </activity>
        <activity
            android:name=".view.activity.profile.ProfileEditActivity"
            android:label="@string/edit_profile_title">
        </activity>
        <activity android:name=".view.activity.profile.ProfileDisplayActivity"
            android:label="@string/profile_display_title">
        </activity>
    </application>

</manifest>